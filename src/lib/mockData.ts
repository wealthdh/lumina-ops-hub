/**
 * DEPRECATED — This file is no longer used.
 *
 * All data now comes from live Supabase queries:
 * - Jobs → ops_jobs table (useJobs hook)
 * - Leads → leads table (useLeads hook)
 * - Tax Pot → tax_pot table (useTaxPot hook)
 * - Briefing → daily_briefings table (useTodayBriefing hook)
 * - Synergies → synergy_links table (useSynergies hook)
 * - Monte Carlo → montecarlo_results table (useMonteCarloResults hook)
 * - Allocations → allocation_rules table (useAllocations hook)
 *
 * This file is intentionally empty. Do not import from it.
 */
